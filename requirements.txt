# --- Real Azure SDKs ---
azure-identity>=1.25.3
azure-ai-projects>=2.2.0
azure-ai-agents>=1.2.0b6
azure-eventhub>=5.15.1
azure-keyvault-secrets>=4.11.0
azure-monitor-opentelemetry>=1.8.8
azure-core>=1.41.0
# Foundry IQ knowledge base (Azure AI Search agentic retrieval). Beta pin —
# the knowledge base / knowledge source APIs are preview.
azure-search-documents>=12.1.0b1

# --- Microsoft Agent Framework (preview) ---
# Pinned loosely; orchestrator falls back to Foundry SDK if not installed.
agent-framework; python_version>="3.10"
# Foundry integration: FoundryChatClient/FoundryAgent, memory, toolbox, evals.
agent-framework-foundry; python_version>="3.10"
# Hosted-agent runtime: ResponsesHostServer serves /responses + /readiness.
# Required inside the Dockerfile.hosted image; optional for local UI demo.
agent-framework-foundry-hosting; python_version>="3.10"
azure-ai-agentserver-responses; python_version>="3.10"

# --- Teams integration ---
pymsteams>=0.2.2
httpx>=0.28.1

# --- Voice Live realtime upstream client ---
websockets>=16.0

# --- Mock D365 (Dataverse-shaped REST) ---
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# --- OpenTelemetry ---
opentelemetry-api==1.40
opentelemetry-sdk==1.40
opentelemetry-instrumentation-httpx==0.61b0

# --- Demo UX & utils ---
rich>=13.9.2
pydantic>=2.9.2
pydantic-settings>=2.14.2
python-dotenv>=1.2.2
typer>=0.12.5
tenacity>=9.0.0
jinja2>=3.1.6

# --- Dev / test ---
pytest>=9.1.1
pytest-asyncio>=1.4.0
